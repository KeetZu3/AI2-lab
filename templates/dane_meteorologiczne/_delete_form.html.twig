<form method="post" action="{{ path('app_dane_meteorologiczne_delete', {'id': dane_meteorologiczne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dane_meteorologiczne.id) }}">
    <button class="btn">Delete</button>
</form>
