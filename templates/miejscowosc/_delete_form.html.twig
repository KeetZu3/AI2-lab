<form method="post" action="{{ path('app_miejscowosc_delete', {'id': miejscowosc.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ miejscowosc.id) }}">
    <button class="btn">Delete</button>
</form>
